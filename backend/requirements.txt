fastapi>=0.109.0
uvicorn>=0.27.0
python-multipart>=0.0.6
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.0
aiofiles>=23.2.1
httpx>=0.26.0
numpy>=1.26.4,<2.1.0
supabase>=2.10.0
reportlab>=4.0.0
pytest>=7.4.4
pytest-asyncio>=0.23.3

# Audio transcription (optional — falls back gracefully if unavailable)
faster-whisper>=1.0.0

# TRIBE v2 (optional - requires GPU)
# tribev2 @ git+https://github.com/facebookresearch/tribev2.git

# MiroFish (optional - self-hosted)
# Runs separately on port 5001
