services:
  - type: web
    name: mirofish-backend
    env: python
    buildCommand: uv sync --frozen
    startCommand: uv run gunicorn -w 1 --threads 8 -b 0.0.0.0:$PORT "app:create_app()"
    envVars:
      - key: FLASK_ENV
        value: production
      - key: FLASK_DEBUG
        value: "false"
